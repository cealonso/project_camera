import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'project_cam2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
